const prod_url = "https://servimate-admin-dev.herokuapp.com/"
const local_url = "http://localhost:4000/"
const partnerWalletActionTypes = [{ "Add": "add" }, { "Subtract": "sub" }, { "Reset": "reset" }]

module.exports = {
    prod_url,
    local_url,
    partnerWalletActionTypes
}